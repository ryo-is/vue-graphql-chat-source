.home
  //- img(src="@/assets/logo.png", alt="Vue logo")
  //- .home-title {{ homeTitle }}
  font-awesome-icon.sign-out(:icon="['fas', 'sign-out-alt']", @click="signOut()")
    //- amplify-sign-out
  .gql-chat-area
    AppSyncComponent(ref="appSync")
